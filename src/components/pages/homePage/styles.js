import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  gridContainer: {
    height: '100vh',
    backgroundColor: '#fff',
  },
}));

export default useStyles;
